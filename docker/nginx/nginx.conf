events {
    worker_connections 1024;
}

http {
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

    client_max_body_size 100m;

    upstream api {
        server api:8000;
    }

    server {
        listen 80;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # CORS is handled by FastAPI's CORSMiddleware in main.py.
        # Do NOT add Access-Control-Allow-Origin here — it would
        # duplicate the header and break browser CORS checks
        # (spec requires exactly one origin per header).
        # Preflight OPTIONS requests pass through to the API,
        # which sets the proper CORS headers on the response.

        location / {
            limit_req zone=api_limit burst=20 nodelay;

            proxy_pass http://api;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location /health {
            proxy_pass http://api;
            access_log off;
        }

    }
}
